return {
  close_on_exit=true,
  float_opts = {
    border = "double",  --options: "single", double", "curved"
    title_pos = "center",
  }

  -- winopts = { number = false, relativenumber = false },
    -- sizes = { sp = 0.5, vsp = 0.4, ["bo sp"] = 0.3, ["bo vsp"] = 0.2 },
    -- float = {
      -- relative = "editor",
      -- row = 0.1,
      -- col = 0.15,
      -- width = 0.75,
      -- height = 0.35,
      -- border = "single",
    -- },
}
